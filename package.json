{
  "name": "prostyle-admin",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5174",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4174",
    "convex:dev": "convex dev",
    "convex:deploy": "convex deploy --yes",
    "convex:prod:state": "convex run --prod prostyle:getState",
    "convex:prod:migrate": "convex run --prod prostyle:migrateAppStateToTables"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "convex": "^1.41.0"
  }
}
